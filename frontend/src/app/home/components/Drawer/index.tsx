import { StyledDrawerContainer } from "./styles"

export const Drawer = () => {
    return (
        <StyledDrawerContainer>
            This is the drawer. It will contain chats and other stuff.
        </StyledDrawerContainer>
    )
}